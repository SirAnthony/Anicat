
export class List extends Router {
    constructor(opt){ super(opt); }
    render(req){
        var self = this;
        var params = this.params(req);
        return when.promise((resolve, reject) => {
        }
    }
}
